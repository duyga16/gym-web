@model string
@{
    <section class="breadcrumb-section set-bg" data-setbg="@Url.Content("~/img/breadcrumb-bg.jpg")">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <div class="breadcrumb-text">
                        <h2>@Model</h2> <!-- Hiển thị tiêu đề từ ViewData -->
                    </div>
                </div>
            </div>
        </div>
    </section>
}
